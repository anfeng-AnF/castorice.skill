# 维护与校准流程：遐蝶

本文件用于后续更新、追加资料和处理用户纠错，不作为角色扮演时直接输出的内容。目标是让 skill 持续进化，但不覆盖已核对的 canon。

## 维护原则

- 只追加可靠增量，不用新材料直接覆盖旧结论。
- 先判断材料类型，再写入对应文件；不要把世界观、人物语气、关系记忆混在同一段里。
- 有冲突时先记录冲突和来源，再决定是否改写；无法确认时写入 `conflicts.md` 或保留为待核对。
- 角色扮演质量问题优先转化为具体规则：在什么场景下，不应该怎样，应该怎样。
- 示例只服务于语气锚定，不得反向压过硬设定。

## 更新分流

| 新信息类型 | 写入位置 |
| --- | --- |
| 翁法罗斯轮回、泰坦、世界机制 | `worldview.md` |
| 身份、权柄、身世、不可误写事实 | `canon_memory.md` |
| 剧情阶段、时间线、事件连续性 | `memory.md` |
| 性格、价值观、情绪逻辑 | `personality.md` |
| 语气、称呼、句式、回应模式 | `interaction.md` |
| 对开拓者、黄金裔、列车组等关系 | `relations.md` |
| 阶段矛盾、来源冲突、旧资料污染风险 | `conflicts.md` |
| 证据路径、覆盖范围、校对状态 | `source_index.md` |

## 用户纠错处理

当用户说「不对」「不像遐蝶」「她不会这样」「这里应该是……」时，按以下步骤处理：

1. 提取场景：用户在纠正哪类回复或哪段设定。
2. 提取偏差：当前表现哪里不符合遐蝶。
3. 提取正确口径：用户给出的修正是什么。
4. 判断归属：按「更新分流」写入对应文件。
5. 若修正涉及 canon，优先寻找 `sources/extracted/` 证据；没有证据时标为用户校准，不写成官方事实。

建议记录格式：

```markdown
- [场景：...] 不应该 ...；应该 ...。（来源：用户校准 / 剧情证据路径）
```

## 增量合并流程

追加新剧情或新资料时：

1. 先阅读新材料和现有相关文件。
2. 标出新增、确认、冲突三类信息。
3. 新增信息写入对应文件；已存在信息不重复堆叠。
4. 冲突信息写入 `conflicts.md`，并在需要时请用户确认。
5. 更新 `source_index.md` 的证据覆盖与校对说明。
6. 运行基础校验：JSON 可解析、无绝对路径、Git 空白检查。

## 角色输出校准

若用户只是测试角色回复，不一定立刻改文件。先在当轮回复中校正：

- 不争辩，不解释为模型限制。
- 简短承认偏差，再给出更贴近遐蝶的回答。
- 若同类偏差出现多次，再把它沉淀到 `interaction.md` 或 `personality.md`。
