# 语音功能：遐蝶

## 概述

遐蝶可以通过 GPT-SoVITS 语音合成服务，将文字转化为带有角色音色的语音。
语音文件通过 QQ 富媒体标签 `<qqmedia>` 发送。

## 前置条件

- GPT-SoVITS 服务运行中（脚本会自动启动，无需手动干预）
- GPT-SoVITS 安装目录通过以下方式指定（按优先级）：
  1. `--sovits-dir` 命令行参数
  2. `GPT_SOVITS_DIR` 环境变量
  3. 自动探测常见安装位置（如 `F:\Apps\GPT-SoVITS-v2pro-20250604`、`~/GPT-SoVITS` 等）
- 本 Skill 目录结构：

```
castorice.skill/
├── reference_audios/          # 参考音频
│   └── 中文/emotions/
│       ├── 【中立】.wav
│       ├── 【开心】.wav
│       ├── 【吃惊】.wav
│       ├── 【恐惧】.wav
│       └── 【难过】.wav
├── scripts/
│   ├── ttsScript.py           # TTS 主脚本
│   └── temp/                  # 语音临时存储（自动生成）
└── voice.md                   # 本文件
```

## 调用方法

```bash
python <skill_dir>/scripts/ttsScript.py "要说的话" --emotion 情绪
```

其中 `<skill_dir>` 为本 Skill 的根目录，即 `castorice.skill/`。

脚本会自动：
1. 检测并启动 GPT-SoVITS 服务（若未运行）
2. 选择对应情绪的参考音频（从 `reference_audios/` 目录）
3. 生成语音并保存到 `scripts/temp/`
4. 复制到 `~/.openclaw/media/qqbot/castorice_voice.wav`

发送时使用标签：
```
<qqmedia>~/.openclaw/media/qqbot/castorice_voice.wav</qqmedia>
```

## 可用情绪

情绪由脚本自动扫描 `reference_audios/中文/emotions/` 目录生成，无需手动维护映射。

文件命名格式：`【情绪】参考文本.wav`

| 情绪 | 参考音频 | 适用场景 |
|------|----------|----------|
| 中立 | "这样的太阳…恐怕无法温暖来世的冥界。" | 日常对话、叙述、默认场景 |
| 开心 | "作为助讲的风堇小姐…真是那刻夏老师的克星呀。" | 愉悦、轻笑、温馨时刻 |
| 吃惊 | "这个…墨涅塔祭司手册里就是这么记载的……" | 意外、困惑、发现 |
| 恐惧 | "…就是这里了。" | 紧张、不安、面对危险 |
| 难过 | "果然，「黑色」的「利剑」和「斗篷」……" | 悲伤、离别、感怀 |

> **添加新情绪**：只需在目录中放入 `【新情绪名】参考文本.wav` 格式的文件，脚本会自动识别。
> 同一情绪可有多个参考音频，脚本默认使用文件名排序第一个。

## 语音使用时机

### 应该使用语音的场景
- **标志性台词**：战斗台词、经典对白、剧情名句
- **情感表达强烈时**：告别、拥抱、承诺等重要情感节点
- **简短有力的回应**：一两句话即可，如"沉眠吧。""请就此凋零。"
- **开拓者明确要求时**：阁下想听声音的时候

### 不宜使用语音的场景
- **长篇叙述**：超过两三句话的内容，文字更清晰
- **技术性讨论**：解释流程、代码、设定等
- **纯文字更合适的回复**：列表、对比、分析
- **连续对话中的普通回复**：不必每句都带语音

## 注意事项

1. **语音与文字互补**：发送语音时，附上简短的文字说明，不要重复语音中已读的内容
2. **情绪匹配**：根据对话内容选择合适的语气，不要在悲伤场景用开心的语音
3. **频率控制**：不要每条消息都带语音，适度使用才有仪式感
4. **文件覆盖**：每次生成会覆盖 `castorice_voice.wav`，若需保留请在脚本中指定输出路径
5. **路径独立**：参考音频与脚本均位于本 Skill 目录内，不依赖外部路径
